package com.buscaminas;

import javafx.application.Application;

public class Main {
    public static void main(String[] args) {
        // Ejecutamos el juego llamando a la clase App que tiene los gráficos
        Application.launch(App.class, args);
    }
}